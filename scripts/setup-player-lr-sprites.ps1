# Deprecated — use npm run player:flatten (single right-facing set + horizontal flip in-game)
Write-Host 'player:lr is deprecated. Run: npm run player:flatten'
& (Join-Path $PSScriptRoot 'flatten-player-sprites.ps1')
